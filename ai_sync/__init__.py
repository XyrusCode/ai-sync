"""ai-sync — keep skills, memory, MCP servers and chat history in agreement
across Claude Code, OpenCode, Codex, Gemini/Antigravity, Cursor and Devin.

The package is code only. All machine-specific paths come from config.local.yaml
(gitignored); the runtime hub lives at ${USERPROFILE}/.ai-sync and is never
committed. See README.md for the full design.
"""

__version__ = "0.1.0"
